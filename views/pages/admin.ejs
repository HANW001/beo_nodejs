<!DOCTYPE html>
<html>
    <head>
        <title>
            <%= title %>
        </title>
        <meta charset="utf-8">
    </head>
    <body>
        sample text
        <a href="/gift/test" method="GET">
            <button type="submit">데이터 불러오기</button>
        </a>
        <a href="/gift/test2" method="GET">
            <button type="submit">리스트 보기</button>
        </a>
        <br />
        <% if(locals.data){ %>
            <% for(let i=0;i<data.length;i++){ %>
                NAME : <%= data[i].id %>, Pasword : <%= data[i].password %><br>
            <% } %>
        <% } %>
        
        <% if(locals.data){ %>
            <% for(let dat in data){ %>
                NAME : <%= dat.name %>, Pasword : <%= dat.age %><br>
            <% } %>
        <% } %>
        
        <% if(locals.data){ %>
            <% data.forEach(function(val){ %>
                NAME : <%= val.name %>, Pasword : <%= val.age %><br>
            <% }) %>
        <% } %>

       

    </body>
</html>

